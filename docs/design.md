# แนวทางดีไซน์ UI — Mangosteen Campus

> ต้นฉบับ wireframe (design canvas): https://claude.ai/artifact/QB2y8Qod4AtkfVVFEoWxaZ
> ไฟล์นี้สรุปสิ่งที่อยู่ใน canvas ให้ session ใหม่ใช้ได้โดยไม่ต้องเปิดลิงก์ — ถ้าขัดกัน ให้ยึด canvas แล้วอัปเดตไฟล์นี้

## หลักการ
- โทนสะอาด เรียบ เน้นใช้งานง่าย (อ้างอิงสไตล์ Mango CMU) ไม่ใช้การ์ดลอย/เงา ใช้เส้นคั่นบาง ๆ แบ่งเนื้อหา
- จุดเด่นของแบรนด์คือ **ตราดอก 6 กลีบ** (กลีบเลี้ยงมังคุด) ใช้เป็นโลโก้ใน sidebar และเป็นลายน้ำจางมุมขวาบนของทุกหน้า
- ข้อความทั้งหมดเป็น translation key (ค่า default = ไทย) — ดู i18n ใน `CLAUDE.md`
- **สไตล์ที่ผู้ใช้เลือก:** dashboard ทั้ง 3 role และหน้า Auth (login, สมัคร, ตั้งรหัสผ่าน, สถานะอีเมล, มือถือ) ใช้ **แบบเดิม** (บอร์ดใน canvas ที่ชื่อต่อท้าย "— เดิม") ไม่ใช้ชุด v2 ที่ปรับใหม่ (เก็บไว้ใน canvas เพื่อเทียบเท่านั้น) ส่วนหน้าฟีเจอร์ที่ออกแบบหลังจากนั้น (จัดการผู้ใช้/ภาคเรียน, รายวิชา, กรอกรหัสเข้าเรียน, แผงกระดิ่ง, มือถือ Admin/Teacher) **คงสไตล์ตามที่ออกแบบไว้ก่อน** ผู้ใช้จะทบทวนภายหลัง — ตอนนี้ระบบจึงมีสองสไตล์ปนกันชั่วคราว

## สี (ตั้งเป็น token ใน Tailwind v4 `@theme`)

| ชื่อที่เสนอ | ค่า | ใช้กับ |
|---|---|---|
| `plum-800` | `#5E2250` | sidebar, ตัวเลขเน้น, ปุ่มหลัก, ลิงก์ |
| `plum-300` | `#CDA9C0` | ข้อความเมนู/รองใน sidebar |
| `plum-50` | `#F7EEF4` | ข้อความบนพื้นม่วง |
| `sage-400` | `#9DBB8F` | ไฮไลต์เมนูที่เลือก + ตราดอกบนพื้นม่วง |
| `paper` | `#FBF9FC` | พื้นหลังหน้า |
| `ink` | `#2A1A2E` | ข้อความหลัก |
| `ink-2` | `#6E5B68` | ข้อความรอง |
| `ink-3` | `#A99AA4` | ข้อความจาง — **contrast ต่ำกว่า 4.5:1 บนพื้น `paper` (คำนวณคร่าว ๆ)** ห้ามใช้กับข้อความเล็กที่จำเป็น; ใช้ราว `#7D6B76` แทน แล้วตรวจซ้ำตอน implement |
| `line` | `#E4DAE0` | เส้นคั่น |

## ตัวอักษร
- หัวข้อ/ตัวเลข: **Taviraj** (serif) น้ำหนัก 500–700
- เนื้อความ/UI: **IBM Plex Sans Thai** น้ำหนัก 400–600
- ตอน implement ควรพิจารณา self-host (`@fontsource`) แทนโหลดจาก Google Fonts — ยังไม่ตัดสินใจ

## โครงหน้า (Dashboard)
- Sidebar ซ้ายกว้าง 264px (พื้น `plum-800`): โลโก้ → เมนูตาม role → โปรไฟล์ + ออกจากระบบ
- เนื้อหาหลัก: คำทักทาย + ปุ่มสลับ TH/EN มุมขวาบน → ตัวเลขสำคัญ 3 ตัวคั่นด้วยเส้นตั้ง → สองคอลัมน์ (รายการหลัก 1.6 : รายการรอง 1)

| Role | เมนู sidebar | ตัวเลขสำคัญ | คอลัมน์หลัก / รอง |
|---|---|---|---|
| Admin | แดชบอร์ด · จัดการผู้ใช้ · สนับสนุนระบบ · สลับมุมมอง · Audit Log | ผู้สอน · ผู้เรียน · วิชาที่เปิดสอน | กิจกรรมล่าสุด / ทางลัด |
| Teacher | แดชบอร์ด · รายวิชาของฉัน | วิชาที่สอน · ผู้เรียน · งานรอตรวจ | งานที่รอตรวจ / รายวิชาของฉัน |
| Student | แดชบอร์ด · รายวิชาของฉัน · ประกาศ | วิชาที่ลงทะเบียน · งานสัปดาห์นี้ · ประกาศยังไม่อ่าน | งานที่ต้องทำเร็ว ๆ นี้ / ประกาศล่าสุด |

## รายการหน้าจอ และสถานะ wireframe

| หน้า | สถานะ |
|---|---|
| Dashboard ทั้ง 3 role (desktop) | มีใน canvas |
| Login / สมัครสมาชิก / ตั้งรหัสผ่าน / สถานะอีเมล (desktop) | มีใน canvas — **ผู้ใช้อนุมัติแล้ว** (ลิงก์ "ลืมรหัสผ่าน" อยู่ใต้ช่องรหัสผ่าน) — ทำแล้วครบ: login, สมัคร, ตรวจสอบอีเมล, ยืนยันอีเมล (c4a), ลืมรหัสผ่าน, ตั้งรหัสผ่านใหม่ (c4b) |
| รายการกฎรหัสผ่านในหน้าตั้งรหัสผ่านใหม่ (4 ข้อใน wireframe) | **ผู้ใช้ตัดสินใจ (ก้อน c4b)**: แสดง 2 ข้อที่ติ๊กสดได้จริง (อย่างน้อย 8 ตัว / ไม่เป็นตัวเลขล้วน) ส่วน "ไม่ใช่รหัสที่คนใช้กัน" และ "ไม่คล้ายชื่อหรืออีเมล" ยังบังคับที่ backend แต่ไม่โชว์ในรายการ (ถูกปฏิเสธแล้วขึ้นข้อความใต้ช่อง); ไม่มีกฎ "ห้าม @ ในรหัสผ่าน" (รหัสนักศึกษา/พนักงานห้ามมี @ อยู่แล้ว ซึ่งเป็นสิ่งที่ใช้แยกอีเมลกับรหัสในช่อง login) |
| หน้าสถานะ (ตรวจสอบอีเมล / ยืนยันแล้ว / ลิงก์ใช้ไม่ได้) ในโครงหน้าจริง | wireframe แสดงเป็นการ์ดลอย — **ผู้ใช้อนุมัติ (ก้อน c4)** ให้วางในคอลัมน์ขวาของโครงหน้าเดียวกับ login (ไม่มีเส้นขอบการ์ด); การ์ด "ลิงก์ใช้ไม่ได้" ของการยืนยันอีเมลเพิ่มช่องกรอกอีเมลเพื่อขอลิงก์ใหม่ (ระบบต้องรู้อีเมล) |
| หน้าสมัคร/ยืนยัน/ลืมรหัส/ตั้งรหัส (มือถือ) | ไม่มี wireframe — **ผู้ใช้อนุมัติ (ก้อน c4)** ให้ใช้รูปแบบเดียวกับ login มือถือ: แถบม่วงด้านบน หัวข้อวางบนแถบ ฟอร์มด้านล่าง |
| Login + Student dashboard (มือถือ) | มีใน canvas — **ผู้ใช้อนุมัติแล้ว** (รวมกระดิ่งแจ้งเตือนบน top bar มือถือ) |
| กระดิ่งแจ้งเตือน (อยู่ข้างปุ่ม TH/EN ของ dashboard ทั้ง 3 role) + แผงรายการแจ้งเตือน | มีใน canvas — **ผู้ใช้อนุมัติแล้ว** |
| Admin: จัดการผู้ใช้ (รายการ + ฟอร์มเพิ่มผู้ใช้), จัดการภาคเรียน (มีเมนู "ภาคเรียน" ใน sidebar) | มีใน canvas — **ผู้ใช้อนุมัติแล้ว** |
| Teacher: รายวิชาของฉัน + สร้างรายวิชา | มีใน canvas — **ผู้ใช้อนุมัติแล้ว** |
| Student: รายวิชาของฉัน + เข้าร่วมด้วยรหัส (มีชุดสถานะสำเร็จ/ไม่พบรหัส/ไม่เปิดให้ลงเอง/อยู่ในรายวิชาแล้ว) | มีใน canvas — **ผู้ใช้อนุมัติแล้ว** |
| Dashboard มือถือของ Admin (+ เมนู drawer) และ Teacher | มีใน canvas — **ผู้ใช้อนุมัติแล้ว** |

### ภาษาดีไซน์ของหน้าที่เพิ่มหลัง wireframe ชุดแรก
- **Sidebar** ไล่เฉดม่วง `#662858 → #4E1B42` มีเส้นขอบใน; เมนูที่เลือกใช้พื้นขาวโปร่ง 10% + แถบเขียวอ่อนด้านซ้าย
- **แผ่นงานหลัก (ตาราง/ฟอร์ม)** = แผ่นขาว ขอบ `#EADFE6` มุมโค้ง 14px เงาโทนม่วงบาง ๆ; **ข้อมูลรอง** = พื้นม่วงอ่อน `#F4ECF2` ไม่มีเงา (ไม่ทำการ์ดหน้าตาเหมือนกันทุกใบ)
- **จุดเด่นเฉพาะหน้า:** แถบเส้นเวลาปีการศึกษา (ภาคเรียน), ช่องรหัสเข้าเรียนแยกตัวอักษรบนแผงม่วง (Student), รหัสเข้าเรียนพร้อมปุ่มคัดลอก (Teacher)
- ปุ่มหลักสีม่วง มีเงาและขอบสว่างด้านบนเล็กน้อย; ช่อง input สูง 44px (มือถือ 48px), พื้นที่กดบนมือถืออย่างน้อย 44px
- หน้าที่ต้อง implement ตามนี้: ดูต้นฉบับใน canvas (ลิงก์ด้านบน) เป็นหลัก

ข้อมูลตัวอย่างใน wireframe (เช่น "1/2568", "โครงสร้างข้อมูลและอัลกอริทึม") เป็นแค่ตัวอย่างภาพ — ข้อมูลทดสอบจริงใช้ Term `1/2569` และวิชา Python พื้นฐาน
